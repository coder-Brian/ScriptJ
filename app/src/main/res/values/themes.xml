<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ScriptJ" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ScriptJ" parent="Base.Theme.ScriptJ" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/darkgray</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/jslp</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.ScriptJ</item>
    </style>

    <style name="Theme.ScriptJ.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ScriptJ.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ScriptJ.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>